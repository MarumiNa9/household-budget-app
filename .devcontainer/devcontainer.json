{
  "name": "Household Budget App",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y fonts-noto-cjk && pip install uv && uv sync --locked"
}