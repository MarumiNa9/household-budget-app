[project]
name = "household-budget-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MarumiNa9", email = "256e141e@gsuite.kobe-u.ac.jp" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
]

[project.scripts]
household-budget-app = "household_budget_app:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
